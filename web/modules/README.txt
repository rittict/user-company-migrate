
# Migrate Users and Companies

This module migrates users and companies from a JSON endpoint into Drupal nodes.

## Dependencies
- migrate
- migrate_plus

## Installation
1. Enable the module:
   ```sh
   drush en migrate_users_companies -y

Drush Command
Run the migrations using the custom Drush command:

drush ucm-import